apiVersion: v1
kind: Service
metadata:
  name: backoffice
spec:
  selector:
    app: backoffice
  ports:
    - protocol: TCP
      port: 80     
      targetPort: 5000
      nodePort: 32001  
  type: NodePort
